# Public repository allowlist.
# Everything is private/local by default. Add a path below only after review.
/*

# Local-only references are intentionally private (also covered by /*).
/.local/
/AGENTS.md
/STATE.md

!/.gitignore
!/README.md
!/CHANGELOG.md
!/VERSION
!/config.example.conf
!/install.command
!/uninstall.command
!/bin/

/bin/*
!/bin/mac-lag-monitor.sh
!/bin/mac-lag-monitorctl.sh
!/bin/check-public-tree.sh
